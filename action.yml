---
name: "SpiceDB Version"
description: "Gets the SpiceDB version"
author: "costap"
inputs:
  version:
    description: "version of SpiceDB to use"
    required: false
    default: "latest"
outputs:
  version:
    description: "the latest SpiceDB version"
runs:
  using: "docker"
  image: "Dockerfile"
